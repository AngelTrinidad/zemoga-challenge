import {StyleSheet} from 'react-native/';

const styles = StyleSheet.create({
  card: {
    borderRadius: 20,
    shadowColor: 'black',
    shadowOpacity: 0.5,
    shadowOffset: {
      width: 0,
      height: 4,
    },
    elevation: 11,
  },
});

export default styles;
