import {StyleSheet} from 'react-native';

import {scaleSpace} from '~helpers';

const styles = StyleSheet.create({
  image: {
    padding: scaleSpace(12),
    width: scaleSpace(20),
    height: scaleSpace(20),
  },
});

export default styles;
