import {StyleSheet} from 'react-native';

import {scaleSpace} from '~helpers';

const styles = StyleSheet.create({
  listContent: {
    paddingVertical: scaleSpace(24),
    paddingHorizontal: scaleSpace(8),
  },
});

export default styles;
